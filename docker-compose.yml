services:
  flask:
    build:
        context: .
        dockerfile: flask.Dockerfile
    command: gunicorn -w 4 --bind 0.0.0.0:5050 app:app
    ports:
        - "5050:5050"
    volumes:
        - .:/flask_app